# Toggle-Theme-App
A simple and clean implementation of a Light/Dark theme toggle built with React’s useContext() and useState() hooks. This project demonstrates how to manage global UI state without prop-drilling and how to structure a reusable context provider.
